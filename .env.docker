# This is used by docker-compose.yml
# Override values if needed

PORT=3000
NODE_ENV=production

# PostgreSQL (matches docker-compose.yml)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=rate_limiter

# Redis (matches docker-compose.yml)
REDIS_HOST=redis
REDIS_PORT=6379

# Rate Limits
TIER_FREE=100
TIER_PRO=1000
TIER_ENTERPRISE=10000